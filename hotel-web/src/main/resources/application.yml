spring:
  thymeleaf:
      cache: false
      encoding: UTF-8
      mode: HTML
  profiles:
    active: dao,redis
  servlet:
    multipart:
      enabled: true
      max-file-size: -1
      max-request-size: 10MB
server:
  port: 8080